# Images folder

Add your signup page images here. The frontend expects these filenames by default:

- signup-hero-1.jpg
- signup-hero-2.jpg
- signup-hero-3.jpg

In production, set `VITE_ASSET_BUCKET` so images resolve from:
`https://storage.googleapis.com/<bucket>/images/<filename>`.
